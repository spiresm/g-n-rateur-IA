import React from 'react';

interface PosterFormProps {
  poster: any;
  isGenerating: boolean;
}

export function PosterForm({ poster, isGenerating }: PosterFormProps) {
  return (
    <>
      {/* 🔽 COLLE ICI LE JSX DU FORMULAIRE
          (tout ce qui était dans PosterGenerator,
           SAUF les hooks et les handlers) */}
    </>
  );
}
